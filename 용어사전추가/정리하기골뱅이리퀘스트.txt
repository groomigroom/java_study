
@RequestMapping

웹 주소(URL)를 이 컨트롤러나 기능과 연결(매핑)해 줍니다.
사용자가 특정 주소로 찾아왔을 때 어떤 코드가 실행될지 정해 줍니다.

예시

@RequestMapping("/")
--> localhost:8080 일 때,
@RequestMapping("/bye")
--> localhost:8080/bye 일 때,

---------------------

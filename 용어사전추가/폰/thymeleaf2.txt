있습니다.

<form:inputText name="userName" value="${user.name}" />
…타임리프 표준 방언을 사용하면 다음과 같은 기능을 구현할 수 있습니다.

<input type="text" name="userName" value="James Carrot" th:value="${user.name}" />
이렇게 하면 브라우저에서 올바르게 표시될 뿐만 아니라
https://docs.spring.io/spring-boot/reference/features/external-config.html



import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.context.properties.ConfigurationProperties;

@ConfigurationProperties("my")
public class MyProperties {

	// fields...

	final MyBean myBean;

	private String name;


	@Autowired
	public MyProperties(MyBean myBean) {
		this.myBean = myBean;
	}

	// getters / setters...


	public String getName() {
		return this.name;
	}

	public void setName(String name) {
		this.name = name;
	}


}
레코드에 생성자 바인딩을 사용할 수 있습니다. 레코드에 생성자가 여러 개 없는 경우에는 사용할 필요가 없습니다 @ConstructorBinding.

Security( 위 예시 와 같이) 생성자에 바인딩된 클래스의 중첩 멤버 도 생성자를 통해 바인딩됩니다.

생성자 바인딩을 사용하려면 클래스가 ` @EnableConfigurationPropertiesuseContainer` 또는 `useContainer` 구성 속성 스캔을 통해 활성화되어 있어야 합니다. 일반적인 Spring 메커니즘으로 생성된 빈(예: ` useContainer` 메서드를 @Component사용하여 생성된 빈 @Bean또는 `useContainer`를 사용하여 로드된 빈 @Import) 에는 생성자 바인딩을 사용할 수 없습니다.
생성자 바인딩을 사용하려면 클래스를 컴파일할 때 `--construction-binding` 옵션을 사용해야 합니다 -parameters. Spring Boot의 Gradle 플러그인을 사용하거나 Maven을 사용하는 경우에는 이 작업이 자동으로 수행됩니다 spring-boot-starter-parent.
Optional`with` 연산자는 @ConfigurationProperties주로 반환 타입으로 사용되도록 설계되었으므로, 설정 속성 주입에는 적합하지 않습니다. 다른 타입의 속성과 일관성을 유지하기 위해, 속성이 선언되었지만 Optional값이 없는 경우 null빈 값이 아닌 특정 Optional값이 바인딩됩니다.
속성 이름에 예약어(예: `#`)를 사용하려면 생성자 매개변수에 어노테이션을 my.service.import사용하십시오 .@Name
@DefaultValue 및 바인딩
기본값은 @DefaultValue생성자 매개변수 및 레코드 구성 요소를 사용하여 지정할 수 있습니다. 변환 서비스는 어노테이션 String값을 누락된 속성의 대상 유형으로 강제 변환하는 데 적용됩니다.

위 예시 에서 MyProperties볼 수 있듯이, 중첩 Security클래스는 매개변수 @DefaultValue("USER")에 대해 를 사용합니다 roles. 즉, security속성이 정의되어 있지만 roles가 정의되어 있지 않은 경우 기본값이 "USER"바인딩됩니다.

예를 들어 다음과 같은 속성들이 있습니다.

속성

YAML

my.service.enabled=true
my.service.security.username=admin
구속될 것입니다new MyProperties(true, null, new Security("admin", null, List.of("USER")))

해당 security속성이 전혀 존재하지 않으면 Security인스턴스는 비어 있게 됩니다 null.

예를 들어 다음과 같은 속성들이 있습니다.

속성

YAML

my.service.enabled=true
구속될 것입니다new MyProperties(true, null, null)

security완전히 기본 인스턴스로 바인딩을 트리거하려면 빈 속성을 정의할 수 있습니다 Security.

YAML의 경우 다음 구문을 사용할 수 있습니다.

my:
  service:
    enabled: true
    security: {}
다음과 같은 Properties기능을 사용할 수 있습니다.

my.service.enabled=true
my.service.security=
구속될 것입니다new MyProperties(true, null, new Security(null, null, List.of("USER")))

속성이 누락된 경우에도 항상 null이 아닌 인스턴스를 바인딩하려면 Security빈 어노테이션을 사용할 수 있습니다 @DefaultValue.

자바

코틀린

	public MyProperties(boolean enabled, InetAddress remoteAddress, @DefaultValue Security security) {
		this.enabled = enabled;
		this.remoteAddress = remoteAddress;
		this.security = security;
	}
Kotlin을 사용할 때는 기본값이 없는 username`and` 매개변수를 nullable로 선언해야 합니다.password
기본값
구성 속성에 정의된 기본값은 반영되지 않습니다 Environment. 위 예시에서 바인딩된 enabled속성은 기본값 입니다 .MyPropertiesmy.servicefalse

하지만 사용자가 해당 속성을 설정하지 않으면 해당 값은 my.service.enabled사용할 수 없습니다 . 구체적으로, 이는 명시적으로 기본값을 제공하지 않고 구성 속성에서 또는 를 자리 표시자로 사용하는 것을 방지합니다. 예를 들어 구현에서 해당 속성에 대해 를 쿼리해야 하는 경우 기본값도 함께 제공해야 합니다.Environmentfalse@Value(${"my.service.enabled"})my.service.enabledEnvironmentCondition

@ConfigurationProperties 어노테이션이 적용된 타입 활성화
Spring Boot는 타입을 바인딩 @ConfigurationProperties하고 이를 빈으로 등록하는 인프라를 제공합니다. 클래스별로 구성 속성을 활성화하거나, 컴포넌트 스캔과 유사한 방식으로 작동하는 구성 속성 스캔 기능을 활성화할 수 있습니다.

때때로 `@` 어노테이션이 있는 클래스는 @ConfigurationProperties스캔에 적합하지 않을 수 있습니다. 예를 들어, 자체 자동 구성을 개발하거나 조건부로 활성화하려는 경우입니다. 이러한 경우에는 `@` 어노테이션을 사용하여 처리할 유형 목록을 지정하세요 . 다음 예제와 같이 @EnableConfigurationProperties모든 클래스에 이 작업을 수행할 수 있습니다 .@Configuration

자바

코틀린

import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Configuration;

@Configuration(proxyBeanMethods = false)
@EnableConfigurationProperties(SomeProperties.class)
public class MyConfiguration {

}
자바

코틀린

import org.springframework.boot.context.properties.ConfigurationProperties;

@ConfigurationProperties("some.properties")
public class SomeProperties {

}
구성 속성 스캔을 사용하려면 @ConfigurationPropertiesScan애플리케이션에 어노테이션을 추가하세요. 일반적으로 어노테이션이 지정된 메인 애플리케이션 클래스에 추가 @SpringBootApplication하지만, 어떤 클래스에도 추가할 수 있습니다 @Configuration. 기본적으로 스캔은 어노테이션을 선언한 클래스의 패키지에서 수행됩니다. 스캔할 특정 패키지를 정의하려면 다음 예제와 같이 지정할 수 있습니다.

자바

코틀린

import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.boot.context.properties.ConfigurationPropertiesScan;

@SpringBootApplication
@ConfigurationPropertiesScan({ "com.example.app", "com.example.another" })
public class MyApplication {

}
@ConfigurationProperties빈이 구성 속성 스캔 또는 어노테이션을 통해 등록될 때 @EnableConfigurationProperties, 빈은 다음과 같은 관례적인 이름을 갖습니다 . ` <prefix>-<fqn><bean_name>`, 여기서 ` <prefix><bean_name>`은 어노테이션에 지정된 환경 키 접두사 @ConfigurationProperties이고 ` <fqn><bean_name>`은 빈의 정규화된 이름입니다. 어노테이션에 접두사가 제공되지 않으면 빈의 정규화된 이름만 사용됩니다.

위 예시에서 해당 빈이 패키지 내에 있다고 가정하면 com.example.app, 빈의 이름은 SomeProperties입니다 some.properties-com.example.app.SomeProperties.

@ConfigurationProperties환경 관련 작업만 수행하고, 특히 컨텍스트에서 다른 빈을 주입하지 않는 것을 권장합니다 . 예외적인 경우에는 세터 주입이나 *Aware프레임워크에서 제공하는 인터페이스(예: EnvironmentAware에 접근해야 하는 경우 Environment)를 사용할 수 있습니다. 생성자를 통해 다른 빈을 주입해야 하는 경우에는 구성 속성 빈에 어노테이션을 추가하고 @ComponentJavaBean 기반 속성 바인딩을 사용해야 합니다.

@ConfigurationProperties 어노테이션이 있는 타입을 사용하기
이러한 구성 방식은 SpringApplication다음 예에서 보여주는 것처럼 외부 YAML 구성과 특히 잘 작동합니다.

my:
  service:
    remote-address: 192.168.1.1
    security:
      username: "admin"
      roles:
      - "USER"
      - "ADMIN"
빈을 사용하려면 @ConfigurationProperties다음 예시와 같이 다른 빈과 동일한 방식으로 주입하면 됩니다.

자바

코틀린

import org.springframework.stereotype.Service;

@Service
public class MyService {

	private final MyProperties properties;

	public MyService(MyProperties properties) {
		this.properties = properties;
	}

	public void openConnection() {
		Server server = new Server(this.properties.getRemoteAddress());
		server.start();
		// ...
	}

	// ...

}
이 기능을 사용하면 @ConfigurationPropertiesIDE에서 사용자 지정 키에 대한 자동 완성 기능을 제공하는 데 사용할 수 있는 메타데이터 파일을 생성할 수도 있습니다. 자세한 내용은 부록을 참조하십시오.
타사 구성
@ConfigurationProperties클래스에 어노테이션을 추가하는 것 외에도 , 공개 @Bean메서드에도 어노테이션을 사용할 수 있습니다. 특히 제어할 수 없는 타사 구성 요소에 속성을 바인딩하려는 경우에 유용합니다.

속성을 사용하여 빈을 구성하려면 다음 예와 같이 빈 등록에 Environment추가하십시오 .@ConfigurationProperties

자바

코틀린

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration(proxyBeanMethods = false)
public class ThirdPartyConfiguration {

	@Bean
	@ConfigurationProperties("another")
	public AnotherComponent anotherComponent() {
		return new AnotherComponent();
	}

}
접두사 로 정의된 모든 JavaBean 속성은 앞의 예제와 유사한 방식으로 another해당 빈에 매핑됩니다 .AnotherComponentSomeProperties

이완 결합
EnvironmentSpring Boot는 속성을 빈에 바인딩할 때 몇 가지 완화된 규칙을 사용하므로 속성 이름과 빈 속성 이름이 @ConfigurationProperties정확히 일치할 필요는 없습니다 . 이러한 특징이 유용한 일반적인 예로는 하이픈으로 구분된 환경 속성(예: ` binds`에 바인딩 ) 및 대문자로 시작하는 환경 속성(예: ` binds`에 바인딩 )이 있습니다.Environmentcontext-pathcontextPathPORTport

예를 들어 다음 @ConfigurationProperties클래스를 생각해 보세요.

자바

코틀린

import org.springframework.boot.context.properties.ConfigurationProperties;

@ConfigurationProperties("my.main-project.person")
public class MyPersonProperties {

	private String firstName;

	public String getFirstName() {
		return this.firstName;
	}

	public void setFirstName(String firstName) {
		this.firstName = firstName;
	}

}
위의 코드를 사용하면 다음과 같은 속성 이름을 모두 사용할 수 있습니다.

표 2. 완화된 결합
재산	메모
my.main-project.person.first-name

.propertiesKebab 케이스는 YAML 파일 에서 사용하기에 적합합니다 .

my.main-project.person.firstName

표준 카멜 케이스 구문입니다.

my.main-project.person.first_name

언더스코어 표기법은 .propertiesYAML 파일에서 사용할 수 있는 대체 형식입니다.

MY_MAINPROJECT_PERSON_FIRSTNAME

시스템 환경 변수를 사용할 때 권장되는 대문자 형식입니다.

prefix주석에 사용할 값은 케밥 케이스(소문자로 작성하고 쉼표로 구분 , 예: ) 형식 이어야 합니다 .-my.main-project.person
표 3. 속성 출처별 완화된 구속 규칙
속성 소스	단순한	목록
속성 파일

카멜 케이스, 케밥 케이스 또는 언더스코어 표기법

표준 목록 구문은 [ ]쉼표로 구분된 값을 사용합니다.

YAML 파일

카멜 케이스, 케밥 케이스 또는 언더스코어 표기법

표준 YAML 목록 구문 또는 쉼표로 구분된 값

환경 변수

대문자 형식에 밑줄을 구분자로 사용합니다( 환경 변수에서 바인딩 참조 ).

숫자 값은 밑줄로 둘러싸여 있습니다( 환경 변수에서 바인딩 참조 ).

시스템 속성

카멜 케이스, 케밥 케이스 또는 언더스코어 표기법

표준 목록 구문은 [ ]쉼표로 구분된 값을 사용합니다.

가능한 경우 속성은 소문자 케밥 형식(예: )으로 저장하는 것이 좋습니다 my.person.first-name=Rod.
바인딩 맵
속성 에 바인딩할 때 Map원래 값을 유지하려면 특수한 대괄호 표기법을 사용해야 할 수 있습니다 key. 키가 괄호로 묶여 있지 않으면 []영숫자 이외의 문자 -또는 .가 제거됩니다.

예를 들어, 다음 속성을 a에 바인딩하는 것을 고려해 보세요 Map<String,String>.

속성

YAML

my.map[/key1]=value1
my.map[/key2]=value2
my.map./key3=value3
YAML 파일의 경우, 키가 제대로 파싱되려면 대괄호를 따옴표로 묶어야 합니다.
위 속성들은 맵에서 , 및 를 Map키로 사용하여 바인딩됩니다 . 슬래시(/)는 대괄호로 묶여 있지 않았기 때문에 제거되었습니다./key1/key2key3key3

스칼라 값에 바인딩할 때, 키에 `null`이 포함된 경우 대괄호 ` .#`로 묶을 필요가 없습니다 []. 스칼라 값에는 열거형과 ` java.langInt` 패키지를 제외한 모든 유형이 포함됩니다 Object. `Int`에 바인딩 a.b=c하면 키의 Map<String, String>`null`이 유지되고 .해당 `Int` 항목이 있는 `Map`이 반환됩니다 . 다른 유형의 경우, 키 에 `null`이 포함되어 {"a.b"="c"}있으면 대괄호 `#`를 사용해야 합니다 . 예를 들어, `Int`에 바인딩하면 `Int` 항목이 있는 `Map`이 반환되고 , `Int`에 바인딩하면 `Int` 항목이 있는 `Map`이 반환됩니다 .key.a.b=cMap<String, Object>{"a"={"b"="c"}}[a.b]=c{"a.b"="c"}

환경 변수로부터의 바인딩
대부분의 운영 체제는 환경 변수에 사용할 수 있는 이름에 엄격한 규칙을 적용합니다. 예를 들어, Linux 셸 변수는 문자(예: ` an` z또는 A`n` Z), 숫자( 예: ` 0n` 또는 ` 9n`) 또는 밑줄 문자( _`)만 포함할 수 있습니다. 관례적으로 Unix 셸 변수의 이름은 대문자로 작성됩니다.

Spring Boot의 완화된 바인딩 규칙은 가능한 한 이러한 명명 제한과 호환되도록 설계되었습니다.

정식 형식의 속성 이름을 환경 변수 이름으로 변환하려면 다음 규칙을 따르세요.

점( )을 밑줄( ) .로 바꿉니다._

하이픈(- -)을 모두 제거하세요.

대문자로 변환하세요.

예를 들어, 구성 속성은 spring.main.log-startup-info라는 이름의 환경 변수가 될 수 있습니다 SPRING_MAIN_LOGSTARTUPINFO.

환경 변수는 객체 목록에 바인딩할 때도 사용할 수 있습니다. 객체 목록에 바인딩하려면 List변수 이름에서 요소 번호를 밑줄로 묶어야 합니다.

예를 들어, 구성 속성은 my.service[0].other라는 이름의 환경 변수를 사용합니다 MY_SERVICE_0_OTHER.

환경 변수로부터의 바인딩 지원은 systemEnvironment속성 소스와 이름이 .으로 끝나는 모든 추가 속성 소스에 적용됩니다 -systemEnvironment.

환경 변수에서 바인딩 맵 사용
Spring Boot는 환경 변수를 속성 클래스에 바인딩할 때 바인딩하기 전에 환경 변수 이름을 소문자로 변환합니다. 대부분의 경우 이 세부 사항은 중요하지 않지만, Map속성에 바인딩할 때는 중요합니다.

Map다음 예시에서 볼 수 있듯이 키는 항상 소문자입니다.

자바

코틀린

import java.util.HashMap;
import java.util.Map;

import org.springframework.boot.context.properties.ConfigurationProperties;

@ConfigurationProperties("my.props")
public class MyMapsProperties {

	private final Map<String, String> values = new HashMap<>();

	public Map<String, String> getValues() {
		return this.values;
	}

}
설정할 때 MY_PROPS_VALUES_KEY=value, 에는 항목 values Map이 포함됩니다 {"key"="value"}.

환경 변수 이름 만 소문자로 표기되고 값은 소문자로 표기되지 않습니다. 설정할 때 MY_PROPS_VALUES_KEY=VALUE, 에는 항목이 values Map포함됩니다 {"key"="VALUE"}.

캐싱
완화된 바인딩은 성능 향상을 위해 캐시를 사용합니다. 기본적으로 이 캐싱은 불변 속성 소스에만 적용됩니다. 예를 들어 가변 속성 소스에 대한 캐싱을 활성화하는 등 이 동작을 사용자 지정하려면 .을 사용하십시오 ConfigurationPropertyCaching.

복합 타입 병합
목록이 여러 곳에 구성되어 있는 경우, 덮어쓰기는 전체 목록을 교체하는 방식으로 작동합니다.

예를 들어, 기본적으로 활성화된 속성을 가진 객체가 있다고 가정해 보겠습니다 . 다음 MyPojo예제 는 해당 객체 의 목록을 보여줍니다 .namedescriptionnullMyPojoMyProperties

자바

코틀린

import java.util.ArrayList;
import java.util.List;

import org.springframework.boot.context.properties.ConfigurationProperties;

@ConfigurationProperties("my")
public class MyProperties {

	private final List<MyPojo> list = new ArrayList<>();

	public List<MyPojo> getList() {
		return this.list;
	}

}
다음 구성을 고려해 보세요.

속성

YAML

my.list[0].name=my name
my.list[0].description=my description
#---
spring.config.activate.on-profile=dev
my.list[0].name=my another name
dev프로필이 활성화되지 않은 경우 , 이전에 정의한 대로 항목이 MyProperties.list하나만 포함됩니다 . 하지만 프로필이 활성화된 경우에도 항목은 하나만 포함됩니다(이름은 , 설명은 ). 이 설정은 목록에 두 번째 항목을 추가 하지 않으며 , 항목을 병합하지도 않습니다.MyPojodevlist my another namenullMyPojo

여러 프로필에서 'a'가 List지정된 경우, 가장 높은 우선순위를 가진 프로필(그리고 오직 그 프로필만)이 사용됩니다. 다음 예를 참조하십시오.

속성

YAML

my.list[0].name=my name
my.list[0].description=my description
my.list[1].name=another name
my.list[1].description=another description
#---
spring.config.activate.on-profile=dev
my.list[0].name=my another name
앞의 예에서 dev프로필이 활성화된 경우, 하나의 항목(이름은 , 설명은 ) MyProperties.list이 포함됩니다 . YAML의 경우, 쉼표로 구분된 목록과 YAML 목록 모두 목록 내용을 완전히 덮어쓰는 데 사용할 수 있습니다. MyPojomy another namenull

속성 의 경우 Map여러 소스에서 가져온 속성 값을 바인딩할 수 있습니다. 단, 여러 소스에서 동일한 속성을 가져오는 경우 우선순위가 가장 높은 소스의 값이 사용됩니다. 다음 예는 다음과 같은 방식을 보여 Map<String, MyPojo>줍니다 MyProperties.

자바

코틀린

import java.util.LinkedHashMap;
import java.util.Map;

import org.springframework.boot.context.properties.ConfigurationProperties;

@ConfigurationProperties("my")
public class MyProperties {

	private final Map<String, MyPojo> map = new LinkedHashMap<>();

	public Map<String, MyPojo> getMap() {
		return this.map;
	}

}
다음 구성을 고려해 보세요.

속성

YAML

my.map.key1.name=my name 1
my.map.key1.description=my description 1
#---
spring.config.activate.on-profile=dev
my.map.key1.name=dev name 1
my.map.key2.name=dev name 2
my.map.key2.description=dev description 2
dev프로필이 활성화되지 않은 경우 , 키 (이름 및 설명 포함) MyProperties.map를 가진 항목이 하나만 있습니다 . 하지만 프로필이 활성화된 경우, 키 (이름 및 설명 포함 )와 (이름 및 설명 포함 )를 가진 항목이 두 개 있습니다.key1my name 1my description 1devmapkey1dev name 1my description 1key2dev name 2dev description 2

앞서 설명한 병합 규칙은 파일뿐만 아니라 모든 속성 소스의 속성에 적용됩니다.
속성 변환
Spring Boot는 외부 애플리케이션 속성을 빈에 바인딩할 때 올바른 유형으로 강제 변환하려고 시도합니다 @ConfigurationProperties. 사용자 지정 유형 변환이 필요한 경우, ConversionService빈(이름이 인 빈 conversionService)이나 사용자 지정 속성 편집기(빈을 통해 CustomEditorConfigurer) 또는 사용자 지정 변환기(빈 정의에 어노테이션이 있는 @ConfigurationPropertiesBinding)를 제공할 수 있습니다.

속성 변환에 사용되는 빈은 애플리케이션 수명 주기 초기에 요청되므로 사용하는 종속성을 제한해야 합니다 ConversionService. 일반적으로 필요한 종속성은 생성 시점에 완전히 초기화되지 않은 경우가 많습니다.

ConversionService구성 키 강제 변환에 필요하지 않고, 특정 조건으로 한정된 사용자 지정 변환기만 사용하는 경우 사용자 지정 변환기의 이름을 변경하는 것이 좋습니다 . 특정 조건으로 메서드를 @ConfigurationPropertiesBinding한정할 때는 "모든 BeanPostProcessors에서 처리될 수 없는 빈입니다"라는 경고를 피하기 위해 메서드 이름이 특정 조건이어야 합니다 .@Bean@ConfigurationPropertiesBindingstatic
기간 변환
Spring Boot는 기간을 표현하기 위한 전용 지원 기능을 제공합니다. 속성을 노출하는 경우 Duration애플리케이션 속성에서 다음과 같은 형식을 사용할 수 있습니다.

일반적인 표현 방식 ( 별도로 지정하지 long않는 한 기본 단위는 밀리초입니다 )@DurationUnit

ISO-8601 표준 형식은 다음과 같은 데 사용됩니다.Duration

값과 단위가 함께 표시된 더 읽기 쉬운 형식입니다( 10s예: 10초).

다음 예를 생각해 보세요.

자바

코틀린

import java.time.Duration;
import java.time.temporal.ChronoUnit;

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.boot.convert.DurationUnit;

@ConfigurationProperties("my")
public class MyProperties {

	@DurationUnit(ChronoUnit.SECONDS)
	private Duration sessionTimeout = Duration.ofSeconds(30);

	private Duration readTimeout = Duration.ofMillis(1000);

	// getters / setters...

	public Duration getSessionTimeout() {
		return this.sessionTimeout;
	}

	public void setSessionTimeout(Duration sessionTimeout) {
		this.sessionTimeout = sessionTimeout;
	}

	public Duration getReadTimeout() {
		return this.readTimeout;
	}

	public void setReadTimeout(Duration readTimeout) {
		this.readTimeout = readTimeout;
	}

}
세션 시간 제한을 30초로 지정하려면 30, PT30S및 30s는 모두 동일합니다. 읽기 시간 제한을 500ms로 지정하려면 다음 형식 중 하나를 사용할 수 있습니다. 500, PT0.5S및 500ms.

지원되는 단위를 사용하셔도 됩니다. 지원되는 단위는 다음과 같습니다.

ns나노초 동안

us마이크로초 동안

ms밀리초 동안

s초 동안

m몇 분 동안

h몇 시간 동안

d며칠 동안

기본 단위는 밀리초이며, @DurationUnit위 예시에서처럼 단위를 변경하여 재정의할 수 있습니다.

생성자 바인딩을 사용하려는 경우 다음 예제와 같이 동일한 속성을 노출할 수 있습니다.

자바

코틀린

import java.time.Duration;
import java.time.temporal.ChronoUnit;

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.boot.context.properties.bind.DefaultValue;
import org.springframework.boot.convert.DurationUnit;

@ConfigurationProperties("my")
public class MyProperties {

	// fields...
	private final Duration sessionTimeout;

	private final Duration readTimeout;

	public MyProperties(@DurationUnit(ChronoUnit.SECONDS) @DefaultValue("30s") Duration sessionTimeout,
			@DefaultValue("1000ms") Duration readTimeout) {
		this.sessionTimeout = sessionTimeout;
		this.readTimeout = readTimeout;
	}

	// getters...

	public Duration getSessionTimeout() {
		return this.sessionTimeout;
	}

	public Duration getReadTimeout() {
		return this.readTimeout;
	}

}
속성 을 업그레이드할 경우 , 단위 가 밀리초가 아니면 Long( )를 사용하여 단위를 정의해야 합니다 . 이렇게 하면 더욱 풍부한 형식을 지원하면서 투명한 업그레이드 경로를 제공할 수 있습니다.@DurationUnit
기간 변환
Spring Boot는 기간 외에도 Period유형을 사용할 수 있습니다. 애플리케이션 속성에서 사용할 수 있는 형식은 다음과 같습니다.

일반적인 표현 방식 ( 별도로 지정하지 int않는 한 기본 단위는 일입니다 )@PeriodUnit

ISO-8601 표준 형식은 다음과 같은 데 사용됩니다.Period

값과 단위가 쌍으로 연결된 더 간단한 형식입니다( 1y3d예: 1년 3일).

다음 단위들이 간편 형식으로 지원됩니다:

y수년간

m몇 달 동안

w몇 주 동안

d며칠 동안

해당 Period데이터 유형은 실제로 주 수를 저장하는 것이 아니라, "7일"을 의미하는 약어입니다.
데이터 크기 변환
Spring Framework는 DataSize크기를 바이트 단위로 표현하는 값 형식을 제공합니다. DataSize속성을 노출하는 경우 애플리케이션 속성에서 다음과 같은 형식을 사용할 수 있습니다.

일반적인 표현 방식 ( 별도로 지정하지 long않는 한 기본 단위는 바이트입니다 )@DataSizeUnit

값과 단위가 함께 표시된 더 읽기 쉬운 형식입니다( 10MB예: 10메가바이트).

다음 예를 생각해 보세요.

자바

코틀린

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.boot.convert.DataSizeUnit;
import org.springframework.util.unit.DataSize;
import org.springframework.util.unit.DataUnit;

@ConfigurationProperties("my")
public class MyProperties {

	@DataSizeUnit(DataUnit.MEGABYTES)
	private DataSize bufferSize = DataSize.ofMegabytes(2);

	private DataSize sizeThreshold = DataSize.ofBytes(512);

	// getters/setters...

	public DataSize getBufferSize() {
		return this.bufferSize;
	}

	public void setBufferSize(DataSize bufferSize) {
		this.bufferSize = bufferSize;
	}

	public DataSize getSizeThreshold() {
		return this.sizeThreshold;
	}

	public void setSizeThreshold(DataSize sizeThreshold) {
		this.sizeThreshold = sizeThreshold;
	}

}
버퍼 크기를 10메가바이트로 지정하려면 10와 는 동일합니다. 크기 임계값 256바이트는 또는 10MB로 지정할 수 있습니다 .256256B

지원되는 단위를 사용하셔도 됩니다. 지원되는 단위는 다음과 같습니다.

B바이트의 경우

KB킬로바이트 단위

MB메가바이트의 경우

GB기가바이트의 경우

TB테라바이트의 경우

기본 단위는 바이트이며, @DataSizeUnit위 예시에서처럼 단위를 변경하여 재정의할 수 있습니다.

생성자 바인딩을 사용하려는 경우 다음 예제와 같이 동일한 속성을 노출할 수 있습니다.

자바

코틀린

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.boot.context.properties.bind.DefaultValue;
import org.springframework.boot.convert.DataSizeUnit;
import org.springframework.util.unit.DataSize;
import org.springframework.util.unit.DataUnit;

@ConfigurationProperties("my")
public class MyProperties {

	// fields...
	private final DataSize bufferSize;

	private final DataSize sizeThreshold;

	public MyProperties(@DataSizeUnit(DataUnit.MEGABYTES) @DefaultValue("2MB") DataSize bufferSize,
			@DefaultValue("512B") DataSize sizeThreshold) {
		this.bufferSize = bufferSize;
		this.sizeThreshold = sizeThreshold;
	}

	// getters...

	public DataSize getBufferSize() {
		return this.bufferSize;
	}

	public DataSize getSizeThreshold() {
		return this.sizeThreshold;
	}

}
속성 을 업그레이드할 경우 , 단위 가 바이트가 아니면 Long(를 사용하여) 단위를 정의해야 합니다 . 이렇게 하면 훨씬 더 풍부한 형식을 지원하면서 투명한 업그레이드 경로를 제공할 수 있습니다.@DataSizeUnit
Base64 데이터 변환
Spring Boot는 Base64로 인코딩된 바이너리 데이터를 해석하는 것을 지원합니다. 속성을 노출할 경우 , 다음 예시와 같이 접두사를 붙여 ResourceBase64로 인코딩된 텍스트를 값으로 제공할 수 있습니다 .base64:

속성

YAML

my.property=base64:SGVsbG8gV29ybGQ=
이 Resource속성은 리소스에 대한 경로를 제공하는 데에도 사용할 수 있으므로 활용도가 더욱 높아집니다.
@ConfigurationProperties 유효성 검사
@ConfigurationPropertiesSpring Boot는 클래스에 Spring 어노테이션이 추가될 때마다 유효성 검사를 시도합니다 @Validated. JSR-303 제약 조건 어노테이션을 구성 클래스에 직접 사용할 수도 있습니다 jakarta.validation. 이를 위해서는 JSR-303을 준수하는 구현체가 클래스패스에 있는지 확인한 다음, 다음 예제와 같이 필드에 제약 조건 어노테이션을 추가하면 됩니다.

자바

코틀린

import java.net.InetAddress;

import jakarta.validation.constraints.NotNull;

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.validation.annotation.Validated;

@ConfigurationProperties("my.service")
@Validated
public class MyProperties {

	@NotNull
	private InetAddress remoteAddress;

	// getters/setters...

	public InetAddress getRemoteAddress() {
		return this.remoteAddress;
	}

	public void setRemoteAddress(InetAddress remoteAddress) {
		this.remoteAddress = remoteAddress;
	}

}
@Bean구성 속성을 생성하는 메서드에 어노테이션을 추가하여 유효성 검사를 트리거할 수도 있습니다 @Validated.
중첩된 속성에 대한 유효성 검사를 적용하려면 관련 필드에 어노테이션을 추가해야 합니다 @Valid. 다음 예제는 앞의 MyProperties예제를 기반으로 합니다.

자바

코틀린

import java.net.InetAddress;

import jakarta.validation.Valid;
import jakarta.validation.constraints.NotEmpty;
import jakarta.validation.constraints.NotNull;

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.validation.annotation.Validated;

@ConfigurationProperties("my.service")
@Validated
public class MyProperties {

	@NotNull
	private InetAddress remoteAddress;

	@Valid
	private final Security security = new Security();

	// getters/setters...

	public InetAddress getRemoteAddress() {
		return this.remoteAddress;
	}

	public void setRemoteAddress(InetAddress remoteAddress) {
		this.remoteAddress = remoteAddress;
	}

	public Security getSecurity() {
		return this.security;
	}

	public static class Security {

		@NotEmpty
		private String username;

		// getters/setters...

		public String getUsername() {
			return this.username;
		}

		public void setUsername(String username) {
			this.username = username;
		}

	}

}
Validator또한 `construction properties validator` 라는 이름의 빈 정의를 생성하여 사용자 지정 Spring 빈을 추가할 수 있습니다 configurationPropertiesValidator. 해당 @Bean메서드는 `static`으로 선언해야 합니다 static. 구성 속성 유효성 검사기는 애플리케이션 수명 주기 초기에 생성되므로, 메서드를 @Bean`static`으로 선언하면 클래스를 인스턴스화하지 않고도 빈을 생성할 수 있습니다 @Configuration. 이렇게 하면 조기 인스턴스화로 인해 발생할 수 있는 문제를 방지할 수 있습니다.

이 spring-boot-actuator모듈에는 모든 빈을 노출하는 엔드포인트가 포함되어 있습니다 @ConfigurationProperties. 웹 브라우저에서 /actuator/configprops해당 엔드포인트로 접속하거나, 이에 상응하는 JMX 엔드포인트를 사용하십시오. 자세한 내용은 프로덕션 환경 기능 섹션을 참조하십시오.
@ConfigurationProperties와 @Value의 차이점
어노테이션 @Value은 컨테이너의 핵심 기능이며, 타입 안전 구성 속성과 동일한 기능을 제공하지는 않습니다. 다음 표는 @ConfigurationProperties어노테이션 에서 지원하는 기능을 요약한 것입니다 @Value.

특징	@ConfigurationProperties	@Value
이완된 결합

예

제한적임 ( 아래 참고사항 참조 )

메타데이터 지원

예

아니요

SpEL평가

아니요

예

만약 `react-binding`을 사용하고 싶다면 , 속성 이름을 표준 형식(소문자만 사용하는 케밥 케이스)으로 참조하는 것이 좋습니다. 이렇게 하면 Spring Boot가 완화된 바인딩을@Value 사용할 때와 동일한 로직을 적용할 수 있습니다 . @ConfigurationProperties

예를 들어, 는 파일과 시스템 환경에서 양식을 가져옵니다. 대신 를 사용하면 파일 과 @Value("${demo.item-price}")시스템 환경에서 양식을 가져 오지만, 는 고려 되지 않습니다.demo.item-pricedemo.itemPriceapplication.propertiesDEMO_ITEMPRICE@Value("${demo.itemPrice}")demo.itemPriceapplication.propertiesDEMO_ITEMPRICEdemo.item-price

컴포넌트에 대한 설정 키 세트를 정의하는 경우, `@Process` 어노테이션이 있는 POJO에 해당 키들을 그룹화하는 것이 좋습니다 @ConfigurationProperties. 이렇게 하면 구조화되고 타입 안정성이 확보된 객체를 생성하여 자체 빈에 주입할 수 있습니다.

SpEL애플리케이션 속성 파일 의 표현식은 해당 파일을 구문 분석하고 환경을 채울 때 처리되지 않습니다. 그러나 SpEL표현식을 직접 작성할 수는 있습니다 @Value. 애플리케이션 속성 파일의 속성 값이 SpEL표현식인 경우, 해당 표현식은 를 통해 사용될 때 평가됩니다 @Value.



자바 스프링에서 private final BookmarkRepository; 와 같이 타입 없이 필드명을 바로 쓰면 문법 오류가 발생합니다. 뒤에 생략된 데이터 타입은 관례상 BookmarkRepository 인터페이스(또는 클래스) 그 자체입니다.원인과 해결오류 발생: 자바는 변수를 만들 때 앞쪽에 반드시 데이터 타입을 적어야 합니다.실제 타입: 코드의 구조상 BookmarkRepository라는 이름의 인터페이스 타입을 뜻합니다.스프링에서의 사용 형태스프링 부트에서는 보통 생성자 주입을 위해 아래와 같이 작성합니다.private final BookmarkRepository bookmarkRepository; (앞에 타입, 뒤에 변수명)
